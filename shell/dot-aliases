#alias aliases='bat ~/.aliases --pager never 2>/dev/null'
alias sa='alias | fzf'
alias se='printenv | fzf'
alias s=sudo
alias svi='sudo vim'
alias apt='sudo apt'
alias p='ping6'
alias q='exit'
alias tf='tail -f'
alias tt='telnet'
alias mtr='mtr --curses'
alias yamllint='yamllint -d ~/.yamllint.conf'

alias stowth='stow --dotfiles --verbose --stow --target ~/'
alias unstowth='stow --dotfiles --verbose --delete --target ~/'

# shutdown
#
alias reboot='sudo shutdown -r now'
alias halt='sudo shutdown -h now'

#case "$OS" in
#   *linux*)
#      alias ll='ls -Fh --color -l'
#      alias la='ls -Fh --color -la'
#      ;;
#   *apple-darwin*)
#      alias ll='ls -lhG'
#      alias la='ls -lahG'
#      ;;
#   *)
#      alias ll='ls -l'
#      alias la='ls -la'
#      ;;
#esac
